const style = {
  display: 'flex',
};

export const ThemeSelector = () => {
  return (
    <div style={style}>
      <small>In a future version you can change the theme of this app</small>
    </div>
  );
};
